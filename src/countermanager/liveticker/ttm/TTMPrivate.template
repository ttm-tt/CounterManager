// Implement a class TTMPrivate as below
package countermanger.liveticker.ttm;

public class TTMPrivate  {
    // Encrypt the password and return it as a (base64 encoded) string
    public static String encryptPassword(String pwd) {
        return pwd;
    }
    
    // Decrypt a (base64 encoded) encrypted password and return it as a string
    public static String decryptPassword(String pwd) {
        return pwd;
    }
}


